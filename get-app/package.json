{
  "name": "get-stackql",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker behind get-stackql.io that redirects callers to the right stackql release asset.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260920.1",
    "typescript": "^5.5.0",
    "wrangler": "^4.135.0"
  }
}
