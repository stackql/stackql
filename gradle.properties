# The stackql-mcp library version (separate from the pinned stackql server
# version, which lives in Pins.kt). Bump on each library release.
stackqlMcpVersion=0.1.0

org.gradle.parallel=true
org.gradle.caching=true
kotlin.code.style=official
