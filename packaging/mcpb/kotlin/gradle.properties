# The library version == the stackql release it embeds. Stamped by
# packaging/mcpb/scripts/render-platforms.sh (make kotlin-manifest VERSION=X.Y.Z);
# do not hand-edit.
version=0.10.601

org.gradle.parallel=true
org.gradle.caching=true
kotlin.code.style=official
